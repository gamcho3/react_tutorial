import React from "react";
import AuthTemplate from "../components/auth/AuthTemplate";

const RegisterPage = () => {
  return <AuthTemplate type="register"></AuthTemplate>;
};

export default RegisterPage;
